# Decisions

## Milestone 1 (scaffolding)
- Split repo into `/frontend` (Vite + React + TS) and `/backend` (ASP.NET Core Controllers) for clear separation.
- Use SQLite with EF Core and an initial migration to keep local setup simple.
